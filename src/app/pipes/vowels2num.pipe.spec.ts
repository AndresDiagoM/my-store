import { Vowels2numPipe } from './vowels2num.pipe';

describe('Vowels2numPipe', () => {
  it('create an instance', () => {
    const pipe = new Vowels2numPipe();
    expect(pipe).toBeTruthy();
  });
});
